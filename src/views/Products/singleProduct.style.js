import styled from "styled-components";
import { palette } from "styled-theme";
import WithDirection from "../../config/withDirection";

const ProductPageWrapper = styled.div`

`;

export default WithDirection(ProductPageWrapper);
